#include <bits/stdc++.h>
#include <iostream>
#include <string>
using namespace std;

typedef long long ll;

void solve() {
}

int main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    ll test_count;
    cin >> test_count;

    while (test_count--) {
        solve();
    }

    return 0;
}
